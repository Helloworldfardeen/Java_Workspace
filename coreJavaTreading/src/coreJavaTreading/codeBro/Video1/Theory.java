package coreJavaTreading.codeBro.Video1;

public class Theory {
	public static void main(String[] args) {
		/*
		 * Thread = A thread of execution in a program (kind of like a virtual CPU) The
		 * JVM Allows an application to have multiple Thread Running Concurrently Each
		 * thread can execute parts of you code in parallel with the main thread each
		 * thread has a priority Thread With Higher Priority are executed in preference
		 * compared to thread with less priority
		 * 
		 * 
		 * The Java Virtual Machine Continue To execute thread until either of the
		 * following occurs..
		 * 
		 * 1.The exit Method of class Runtime Has been Called. 
		 * 2.All user threads have
		 * Dies.
		 * 
		 * When JVM Starts UPp , there is a thread which calls the main method 
		 * this thread is called "main"
		 * 
		 * Deamon thread is low Priority thread that run in background  to perform 
		 * such as garbage collection
		 * 
		 * JVM terminated itself when all user thread (NON- deamon thread )finish their execution
		 *last me execute hotey deamon thread jab java terminated itself
		 */
	}

}
