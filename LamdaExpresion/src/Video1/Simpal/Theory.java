package Video1.Simpal;

public class Theory {
	/*Lambda Expression = feature for java 8 and above Also Knows as an anonymous classes with
	 * one method 
	 * 
	 * need to use a function interface or use a predefined function interface they contains only
	 * one abstract method 
	 * Ex. ActionListener, Runnable ,(User-defined)
	 * A Lambda Expression can be used In any place where a function interface
	 * how to use a lambda expression :
	 * (argument)->{statement/s};
	 * 
	 * */

}
